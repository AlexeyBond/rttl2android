<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorAccent">@color/accent</item>
    </style>

    <style name="GuyButton_NORMALNIYPAREN" parent="Base.TextAppearance.AppCompat.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:colorBackground">@color/eva_btn</item>
        <item name="android:text">New Button</item>
        <item name="android:width">40dp</item>

    </style>
    <style name="GayButton_IHATEDIS" parent="GuyButton_NORMALNIYPAREN">
        <item name="android:textColor">@color/accent</item>
    </style>

</resources>
